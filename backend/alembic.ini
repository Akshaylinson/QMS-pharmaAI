[alembic]
script_location = alembic
# Alembic is launched by its console script, whose import path does not always
# include the working directory (notably in the backend Docker container).
prepend_sys_path = .
sqlalchemy.url = postgresql+psycopg://qms:qms@db:5432/qms
